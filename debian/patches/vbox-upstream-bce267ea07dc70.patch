vboxmail should reside in /usr/sbin not /usr/bin

picked from upstream bce267ea07dc70fc8015a4629277431c89ca7443
patch can be dropped after next upstream release

diff --git a/vbox/Makefile.in b/vbox/Makefile.in
index a0ecdcf..54666e3 100644
--- a/vbox/Makefile.in
+++ b/vbox/Makefile.in
@@ -270,8 +270,8 @@ scripts-d:	  ignore
 		  @cd $(TOPDIR)/utils && $(MAKE) distclean
 
 scripts-i:	  ignore
-		  @echo "Installing '$(DESTDIR)$(bindir)/vboxmail'..."
-		  @$(INSTALL_SHBIN) $(TOPDIR)/utils/vboxmail $(DESTDIR)$(bindir)/vboxmail
+		  @echo "Installing '$(DESTDIR)$(sbindir)/vboxmail'..."
+		  @$(INSTALL_SHBIN) $(TOPDIR)/utils/vboxmail $(DESTDIR)$(sbindir)/vboxmail
 		  @echo "Installing '$(DESTDIR)$(bindir)/vboxplay'..."
 		  @$(INSTALL_SHBIN) $(TOPDIR)/utils/vboxplay $(DESTDIR)$(bindir)/vboxplay
 
